function getComputerChoice{
    
    const option = ["Rock", "Paper", "Scissors"];
    const randomIndex = Math.floor(Math.random() * 3);
    const randomOptions = option[randomIndex]
    console.log(randomOptions)
}
    
